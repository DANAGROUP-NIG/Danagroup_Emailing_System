# Postfix transport map: route all @danagroup.net mail to the DIMS pipe.
# After editing, rebuild the db: postmap /etc/postfix/transport
#
# Format: <pattern>   <transport>:<nexthop>
danagroup.net   dims:
