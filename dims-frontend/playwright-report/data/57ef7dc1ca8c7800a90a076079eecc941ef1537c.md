# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: admin-rbac.spec.ts >> Admin RBAC >> admin can access admin panel
- Location: e2e/admin-rbac.spec.ts:25:7

# Error details

```
Test timeout of 30000ms exceeded.
```

```
Error: expect(page).toHaveURL(expected) failed

Expected pattern: /.*mail\/inbox.*/
Received string:  "http://localhost:3000/login"

Call log:
  - Expect "toHaveURL" with timeout 15000ms
    29 × unexpected value "http://localhost:3000/login"

```

```yaml
- img "Dana Group logo"
- text: Dana Group
- paragraph: Internal Communication. Reimagined.
- paragraph: DIMS keeps every message, announcement, and conversation across Dana Group subsidiaries in one secure place.
- paragraph: © 2026 Dana Group. All rights reserved.
- heading "Welcome back" [level=1]
- paragraph: Sign in to your DIMS account to continue.
- form "Sign in to DIMS":
  - text: Email address
  - textbox "Email address":
    - /placeholder: you@danagroup.internal
    - text: admin@dana.com
  - text: Password
  - textbox "Password":
    - /placeholder: Enter your password
    - text: password123
  - button "Show password":
    - img
  - checkbox "Remember me"
  - text: Remember me
  - link "Forgot password?":
    - /url: /forgot-password
  - button "Sign in"
- region "Notifications (F8)":
  - list
- region "Notifications (F8)":
  - list
- alert
```

# Test source

```ts
  1  | import { test, expect } from "@playwright/test";
  2  | import {
  3  |   setupAuthMocks,
  4  |   EMPLOYEE_USER,
  5  |   ADMIN_USER,
  6  |   SUBSIDIARY_ADMIN_USER,
  7  | } from "./helpers/mock-api";
  8  | 
  9  | test.describe("Admin RBAC", () => {
  10 |   test("employee cannot access admin users page", async ({ page }) => {
  11 |     await setupAuthMocks(page, EMPLOYEE_USER);
  12 | 
  13 |     // Login as employee
  14 |     await page.goto("/login");
  15 |     await page.fill("input[type='email']", "john.doe@dana.com");
  16 |     await page.fill("input[type='password']", "password123");
  17 |     await page.click("button[type='submit']");
  18 |     await expect(page).toHaveURL(/.*mail\/inbox.*/, { timeout: 15000 });
  19 | 
  20 |     // Try to access admin page — middleware redirects employees back to inbox
  21 |     await page.goto("/admin/users");
  22 |     await expect(page).toHaveURL(/.*mail\/inbox.*/, { timeout: 10000 });
  23 |   });
  24 | 
  25 |   test("admin can access admin panel", async ({ page }) => {
  26 |     await setupAuthMocks(page, ADMIN_USER);
  27 | 
  28 |     // Login as admin
  29 |     await page.goto("/login");
  30 |     await page.fill("input[type='email']", "admin@dana.com");
  31 |     await page.fill("input[type='password']", "password123");
  32 |     await page.click("button[type='submit']");
> 33 |     await expect(page).toHaveURL(/.*mail\/inbox.*/, { timeout: 15000 });
     |                        ^ Error: expect(page).toHaveURL(expected) failed
  34 | 
  35 |     // Navigate to admin
  36 |     await page.goto("/admin/users");
  37 |     await expect(page).toHaveURL(/.*admin\/users.*/, { timeout: 10000 });
  38 | 
  39 |     // Should see admin content
  40 |     await expect(page.locator("[data-testid='admin-panel']")).toBeVisible({ timeout: 10000 });
  41 |   });
  42 | 
  43 |   test("subsidiary admin can access admin panel", async ({ page }) => {
  44 |     await setupAuthMocks(page, SUBSIDIARY_ADMIN_USER);
  45 | 
  46 |     // Login as subsidiary admin
  47 |     await page.goto("/login");
  48 |     await page.fill("input[type='email']", "bob.johnson@dana.com");
  49 |     await page.fill("input[type='password']", "password123");
  50 |     await page.click("button[type='submit']");
  51 |     await expect(page).toHaveURL(/.*mail\/inbox.*/, { timeout: 15000 });
  52 | 
  53 |     // Navigate to admin
  54 |     await page.goto("/admin/users");
  55 |     await expect(page).toHaveURL(/.*admin\/users.*/, { timeout: 10000 });
  56 | 
  57 |     // Should see admin content
  58 |     await expect(page.locator("[data-testid='admin-panel']")).toBeVisible({ timeout: 10000 });
  59 |   });
  60 | });
  61 | 
```