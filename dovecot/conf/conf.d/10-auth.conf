## Authentication configuration

auth_mechanisms = plain login

# Disable system auth — only SQL
!include auth-sql.conf.ext

service auth {
  unix_listener auth-userdb {
    mode = 0600
    user = vmail
  }
}
